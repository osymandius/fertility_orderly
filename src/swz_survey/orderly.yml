script: swz_survey.R

parameters:
  version:
    default: 2022

artefacts:
  - data:
      description: survey indicator dataset
      filenames:
        - swz_dhs_clusters.csv
        - swz_mics_women.csv
        - swz_mics_births_to_women.csv
  - staticgraph:
      description: Check DHS region cluster assignment
      filenames:
        - check/swz_dhs-cluster-check.pdf


packages:
  - dplyr
  - dfertility
  - haven
  - tidyr
  - rdhs
  - ggplot2
  - naomi
  - naomi.utils
  - tidyverse
  - sf
  - spud
  - stringr

global_resources:
  resources/MICS_indicators.csv: MICS_indicators.csv

depends:
  aaa_pull_naomi_estimates:
    id: latest(parameter:version == version && parameter:iso3 == "SWZ")
    use:
      depends/swz_areas.geojson: outputs/naomi_areas.geojson