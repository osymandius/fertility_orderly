script: NAM_asfr.R

artefacts:
  - data:
      description: NAM ASFR dataset
      filenames:
        - NAM_dhs_asfr.csv

packages:
  - demogsurv
  - dplyr
  - rdhs
  - pointr
  - countrycode
  - dfertility

depends:
  data-survey:
    id: latest
      use:
        depends/NAM_dhs_survey.csv: NAM_dhs_survey.csv