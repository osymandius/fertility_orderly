script: nam_survey.R

parameters:
  version:
    default: 2022

artefacts:
  - data:
      description: survey indicator dataset
      filenames:
        - nam_dhs_clusters.csv
  - staticgraph:
      description: Check DHS region cluster assignment
      filenames:
        - check/nam_dhs-cluster-check.pdf


packages:
  - dplyr
  - ggplot2
  - haven
  - naomi
  - naomi.utils
  - tidyverse
  - rdhs
  - sf
  - tidyverse

global_resources:
  resources/areas.rds: areas.rds
  
depends:
  # aaa_areas_pull:
    # id: latest(parameter:version == 2021 && parameter:iso3 == "NAM")
    # use:
      # depends/nam_areas.geojson: naomi_areas.geojson