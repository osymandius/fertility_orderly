script: rwa_survey.R

artefacts:
  - data:
      description: Survey clusters dataset
      filenames:
        - rwa_dhs_clusters.csv

displayname: Rwanda DHS survey clusters

packages:
  - dplyr
  - ggplot2
  - haven
  - naomi
  - naomi.utils
  - rdhs
  - sf

depends:
  rwa_data_areas:
    id: latest
    use:
      depends/rwa_areas.geojson: rwa_areas.geojson
