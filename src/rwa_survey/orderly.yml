script: rwa_survey.R

parameters:
  version:
    default: 2022

artefacts:
  - data:
      description: survey indicator dataset
      filenames:
        - rwa_dhs_clusters.csv
  - staticgraph:
      description: Check DHS region cluster assignment
      filenames:
        - check/rwa_dhs-cluster-check.pdf


packages:
  - dplyr
  - ggplot2
  - haven
  - naomi
  - naomi.utils
  - tidyverse
  - rdhs
  - sf
  - tidyverse


global_resources:
  resources/areas.rds: areas.rds
  
depends:
  # aaa_areas_pull:
    # id: latest(parameter:version == 2021 && parameter:iso3 == "RWA")
    # use:
      # depends/rwa_areas.geojson: naomi_areas.geojson