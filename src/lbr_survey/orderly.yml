script: lbr_survey.R

parameters:
  version:
    default: 2022

artefacts:
  - data:
      description: survey indicator dataset
      filenames:
        - lbr_dhs_clusters.csv
  - staticgraph:
      description: Check DHS region cluster assignment
      filenames:
        - check/lbr_dhs-cluster-check.pdf


packages:
  - dplyr
  - dfertility
  - haven
  - tidyr
  - rdhs
  - ggplot2
  - naomi
  - naomi.utils
  - tidyverse
  - sf
  - spud
  - stringr

global_resources:
  resources/areas.rds: areas.rds
  
depends:
  # aaa_areas_pull:
    # id: latest(parameter:version == 2021 && parameter:iso3 == "LBR")
    # use:
      # depends/lbr_areas.geojson: naomi_areas.geojson