script: bwa_survey.R

parameters:
  version:
    default: 2022

artefacts:
  - data:
      description: survey indicator dataset
      filebwaes:
        - bwa_dhs_clusters.csv
  - staticgraph:
      description: Check DHS region cluster assignment
      filebwaes:
        - check/bwa_dhs-cluster-check.pdf


packages:
  - dplyr
  - ggplot2
  - haven
  - naomi
  - naomi.utils
  - tidyverse
  - rdhs
  - sf
  - tidyverse

global_resources:
  resources/areas.rds: areas.rds
  
depends:
  # aaa_areas_pull:
    # id: latest(parameter:version == 2021 && parameter:iso3 == "BWA")
    # use:
      # depends/bwa_areas.geojson: naomi_areas.geojson