script: mli_survey.R

parameters:
  version:
    default: 2022

artefacts:
  - data:
      description: survey indicator dataset
      filenames:
        - mli_dhs_clusters.csv
        - mli_mics_women.csv
        - mli_mics_births_to_women.csv
  - staticgraph:
      description: Check DHS region cluster assignment
      filenames:
        - check/mli_dhs-cluster-check.pdf


packages:
  - dplyr
  - dfertility
  - haven
  - tidyr
  - rdhs
  - ggplot2
  - naomi
  - naomi.utils
  - tidyverse
  - sf
  - spud
  - stringr

global_resources:
  resources/MICS_indicators.csv: MICS_indicators.csv

depends:
  aaa_areas_pull:
    id: latest(parameter:version == 2021 && parameter:iso3 == "MLI")
    use:
      depends/mli_areas.geojson: naomi_areas.geojson